[package]
name = "kyerag"
version.workspace = true
edition.workspace = true

[dependencies]
kyerag-render = { workspace = true }
libcosmic = { workspace = true }
# Screenshots (issue #15): where a still goes, and what it is encoded as.
dirs = { workspace = true }
png = { workspace = true }
